#set("title") { Martin Lasek }

#set("content") {
    <div class="projects-site">
        #for(project in projectList) {
          #embed("partials/project")
        }
    </div>
}

#embed("Layout/base")
